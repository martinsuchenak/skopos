{
  "_comment": "Merge the hooks section into ~/.claude/settings.json. Replace SKOPOS_HOOKS_PATH with the absolute path to docs/integrations/claude-code/hooks.sh. MCP servers do NOT belong in settings.json — add the mcpServers entry to ~/.claude.json (user scope) or .mcp.json in the repo root (project scope) instead; see README Step 1.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash SKOPOS_HOOKS_PATH pre-tool"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash SKOPOS_HOOKS_PATH post-tool"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash SKOPOS_HOOKS_PATH stop"
          }
        ]
      }
    ]
  }
}
