{
  "_comment": "Merge into .vscode/tasks.json in your workspace. Runs skopos report automatically when VS Code opens the folder.",
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Skopos: report session start",
      "type": "shell",
      "command": "skopos report --agent-id \"github-copilot-$(hostname -s)\" --agent-type github-copilot --workspace \"${workspaceFolder}\" --status running --message \"session started\" || true",
      "runOptions": {
        "runOn": "folderOpen"
      },
      "presentation": {
        "reveal": "never",
        "panel": "shared"
      }
    }
  ]
}
