{
  "_comment": "Add the 'skopos' entry under mcpServers in ~/.gemini/settings.json. Use httpUrl (not url) — in Gemini CLI 'url' selects the SSE transport while 'httpUrl' is streamable HTTP, which is what skopos serves at /mcp. The Authorization header is only required when auth.api_key is set on the server.",
  "mcpServers": {
    "skopos": {
      "httpUrl": "http://localhost:8080/mcp",
      "trust": true,
      "headers": {
        "Authorization": "Bearer ${SKOPOS_API_KEY}"
      }
    }
  }
}
